syntax = "proto3";

package udb.repro.v1;

import "udb/core/common/v1/db.proto";

message FormatterRepro {
  string state = 1 [
    (udb.core.common.v1.pg_column) = {
      column_name: "state"
      sql_type: "TEXT"
      default_value: "'draft  review , pending'"
    }
  ];
}
