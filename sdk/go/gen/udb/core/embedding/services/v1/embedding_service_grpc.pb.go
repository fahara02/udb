// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: udb/core/embedding/services/v1/embedding_service.proto

package servicesv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	EmbeddingService_RegisterSource_FullMethodName  = "/udb.core.embedding.services.v1.EmbeddingService/RegisterSource"
	EmbeddingService_ListSources_FullMethodName     = "/udb.core.embedding.services.v1.EmbeddingService/ListSources"
	EmbeddingService_DeleteSource_FullMethodName    = "/udb.core.embedding.services.v1.EmbeddingService/DeleteSource"
	EmbeddingService_Backfill_FullMethodName        = "/udb.core.embedding.services.v1.EmbeddingService/Backfill"
	EmbeddingService_ReportEmbedding_FullMethodName = "/udb.core.embedding.services.v1.EmbeddingService/ReportEmbedding"
	EmbeddingService_Retrieve_FullMethodName        = "/udb.core.embedding.services.v1.EmbeddingService/Retrieve"
)

// EmbeddingServiceClient is the client API for EmbeddingService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// EmbeddingService (master-plan 9.11) — the AI data plane. Registers tenant-scoped
// source entities to vector-index on change. INFERENCE RUNS IN SIDECARS ONLY: no
// embedding model is ever linked into the broker. On a source row change (and on
// Backfill) the broker emits a `udb.embedding.work.v1` event carrying ONLY the row
// primary key + extracted text (NEVER credentials); a sidecar computes the vector
// and returns it via the internal-only `ReportEmbedding` callback, which upserts
// it through the shared asset vector-upsert seam tagged with the verified tenant.
// `Retrieve` delegates to the SearchService (9.5) hybrid-search seam with a
// server-side tenant filter — never a raw vector query.
type EmbeddingServiceClient interface {
	// Register a tenant-scoped source to vector-index on change. Fails closed
	// (failed_precondition) when the source table has no resolvable tenant column.
	RegisterSource(ctx context.Context, in *RegisterSourceRequest, opts ...grpc.CallOption) (*RegisterSourceResponse, error)
	// List the calling tenant's registered sources.
	ListSources(ctx context.Context, in *ListSourcesRequest, opts ...grpc.CallOption) (*ListSourcesResponse, error)
	// Delete a tenant-scoped source registration (destructive: stops indexing on
	// change; the engine collection teardown runs on the follow-up worker).
	DeleteSource(ctx context.Context, in *DeleteSourceRequest, opts ...grpc.CallOption) (*DeleteSourceResponse, error)
	// Enqueue embedding work for the source's EXISTING rows. The per-row work
	// enumeration runs in the leader-spawned work emitter, which calls the same
	// `udb.embedding.work.v1` emit path the CDC change handler uses.
	Backfill(ctx context.Context, in *BackfillRequest, opts ...grpc.CallOption) (*BackfillResponse, error)
	// SIDECAR CALLBACK (internal only). A sidecar that computed an embedding for a
	// source row returns the dense vector here; the broker upserts it through the
	// shared asset vector-upsert seam, tagged with the VERIFIED claim tenant (a
	// vector with no/foreign tenant is rejected — no fail-open). `internal_grpc_only`
	// restricts this to a loopback peer; it is never exposed in an SDK facade.
	ReportEmbedding(ctx context.Context, in *ReportEmbeddingRequest, opts ...grpc.CallOption) (*ReportEmbeddingResponse, error)
	// Deadline-bounded semantic search over a source's vector collection. DELEGATES
	// to the SearchService (9.5) hybrid-search seam with a server-side tenant filter
	// injected from the verified claim. The broker never embeds the query (the
	// caller supplies an already-embedded `query_vector`); it never issues a raw
	// engine query. Returns `deadline_exceeded` if the gRPC deadline is past.
	Retrieve(ctx context.Context, in *RetrieveRequest, opts ...grpc.CallOption) (*RetrieveResponse, error)
}

type embeddingServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewEmbeddingServiceClient(cc grpc.ClientConnInterface) EmbeddingServiceClient {
	return &embeddingServiceClient{cc}
}

func (c *embeddingServiceClient) RegisterSource(ctx context.Context, in *RegisterSourceRequest, opts ...grpc.CallOption) (*RegisterSourceResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RegisterSourceResponse)
	err := c.cc.Invoke(ctx, EmbeddingService_RegisterSource_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *embeddingServiceClient) ListSources(ctx context.Context, in *ListSourcesRequest, opts ...grpc.CallOption) (*ListSourcesResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ListSourcesResponse)
	err := c.cc.Invoke(ctx, EmbeddingService_ListSources_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *embeddingServiceClient) DeleteSource(ctx context.Context, in *DeleteSourceRequest, opts ...grpc.CallOption) (*DeleteSourceResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(DeleteSourceResponse)
	err := c.cc.Invoke(ctx, EmbeddingService_DeleteSource_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *embeddingServiceClient) Backfill(ctx context.Context, in *BackfillRequest, opts ...grpc.CallOption) (*BackfillResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(BackfillResponse)
	err := c.cc.Invoke(ctx, EmbeddingService_Backfill_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *embeddingServiceClient) ReportEmbedding(ctx context.Context, in *ReportEmbeddingRequest, opts ...grpc.CallOption) (*ReportEmbeddingResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ReportEmbeddingResponse)
	err := c.cc.Invoke(ctx, EmbeddingService_ReportEmbedding_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *embeddingServiceClient) Retrieve(ctx context.Context, in *RetrieveRequest, opts ...grpc.CallOption) (*RetrieveResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RetrieveResponse)
	err := c.cc.Invoke(ctx, EmbeddingService_Retrieve_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// EmbeddingServiceServer is the server API for EmbeddingService service.
// All implementations should embed UnimplementedEmbeddingServiceServer
// for forward compatibility.
//
// EmbeddingService (master-plan 9.11) — the AI data plane. Registers tenant-scoped
// source entities to vector-index on change. INFERENCE RUNS IN SIDECARS ONLY: no
// embedding model is ever linked into the broker. On a source row change (and on
// Backfill) the broker emits a `udb.embedding.work.v1` event carrying ONLY the row
// primary key + extracted text (NEVER credentials); a sidecar computes the vector
// and returns it via the internal-only `ReportEmbedding` callback, which upserts
// it through the shared asset vector-upsert seam tagged with the verified tenant.
// `Retrieve` delegates to the SearchService (9.5) hybrid-search seam with a
// server-side tenant filter — never a raw vector query.
type EmbeddingServiceServer interface {
	// Register a tenant-scoped source to vector-index on change. Fails closed
	// (failed_precondition) when the source table has no resolvable tenant column.
	RegisterSource(context.Context, *RegisterSourceRequest) (*RegisterSourceResponse, error)
	// List the calling tenant's registered sources.
	ListSources(context.Context, *ListSourcesRequest) (*ListSourcesResponse, error)
	// Delete a tenant-scoped source registration (destructive: stops indexing on
	// change; the engine collection teardown runs on the follow-up worker).
	DeleteSource(context.Context, *DeleteSourceRequest) (*DeleteSourceResponse, error)
	// Enqueue embedding work for the source's EXISTING rows. The per-row work
	// enumeration runs in the leader-spawned work emitter, which calls the same
	// `udb.embedding.work.v1` emit path the CDC change handler uses.
	Backfill(context.Context, *BackfillRequest) (*BackfillResponse, error)
	// SIDECAR CALLBACK (internal only). A sidecar that computed an embedding for a
	// source row returns the dense vector here; the broker upserts it through the
	// shared asset vector-upsert seam, tagged with the VERIFIED claim tenant (a
	// vector with no/foreign tenant is rejected — no fail-open). `internal_grpc_only`
	// restricts this to a loopback peer; it is never exposed in an SDK facade.
	ReportEmbedding(context.Context, *ReportEmbeddingRequest) (*ReportEmbeddingResponse, error)
	// Deadline-bounded semantic search over a source's vector collection. DELEGATES
	// to the SearchService (9.5) hybrid-search seam with a server-side tenant filter
	// injected from the verified claim. The broker never embeds the query (the
	// caller supplies an already-embedded `query_vector`); it never issues a raw
	// engine query. Returns `deadline_exceeded` if the gRPC deadline is past.
	Retrieve(context.Context, *RetrieveRequest) (*RetrieveResponse, error)
}

// UnimplementedEmbeddingServiceServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedEmbeddingServiceServer struct{}

func (UnimplementedEmbeddingServiceServer) RegisterSource(context.Context, *RegisterSourceRequest) (*RegisterSourceResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method RegisterSource not implemented")
}
func (UnimplementedEmbeddingServiceServer) ListSources(context.Context, *ListSourcesRequest) (*ListSourcesResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ListSources not implemented")
}
func (UnimplementedEmbeddingServiceServer) DeleteSource(context.Context, *DeleteSourceRequest) (*DeleteSourceResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method DeleteSource not implemented")
}
func (UnimplementedEmbeddingServiceServer) Backfill(context.Context, *BackfillRequest) (*BackfillResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Backfill not implemented")
}
func (UnimplementedEmbeddingServiceServer) ReportEmbedding(context.Context, *ReportEmbeddingRequest) (*ReportEmbeddingResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ReportEmbedding not implemented")
}
func (UnimplementedEmbeddingServiceServer) Retrieve(context.Context, *RetrieveRequest) (*RetrieveResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Retrieve not implemented")
}
func (UnimplementedEmbeddingServiceServer) testEmbeddedByValue() {}

// UnsafeEmbeddingServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to EmbeddingServiceServer will
// result in compilation errors.
type UnsafeEmbeddingServiceServer interface {
	mustEmbedUnimplementedEmbeddingServiceServer()
}

func RegisterEmbeddingServiceServer(s grpc.ServiceRegistrar, srv EmbeddingServiceServer) {
	// If the following call panics, it indicates UnimplementedEmbeddingServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&EmbeddingService_ServiceDesc, srv)
}

func _EmbeddingService_RegisterSource_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RegisterSourceRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(EmbeddingServiceServer).RegisterSource(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: EmbeddingService_RegisterSource_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(EmbeddingServiceServer).RegisterSource(ctx, req.(*RegisterSourceRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _EmbeddingService_ListSources_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ListSourcesRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(EmbeddingServiceServer).ListSources(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: EmbeddingService_ListSources_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(EmbeddingServiceServer).ListSources(ctx, req.(*ListSourcesRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _EmbeddingService_DeleteSource_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(DeleteSourceRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(EmbeddingServiceServer).DeleteSource(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: EmbeddingService_DeleteSource_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(EmbeddingServiceServer).DeleteSource(ctx, req.(*DeleteSourceRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _EmbeddingService_Backfill_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(BackfillRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(EmbeddingServiceServer).Backfill(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: EmbeddingService_Backfill_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(EmbeddingServiceServer).Backfill(ctx, req.(*BackfillRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _EmbeddingService_ReportEmbedding_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ReportEmbeddingRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(EmbeddingServiceServer).ReportEmbedding(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: EmbeddingService_ReportEmbedding_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(EmbeddingServiceServer).ReportEmbedding(ctx, req.(*ReportEmbeddingRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _EmbeddingService_Retrieve_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RetrieveRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(EmbeddingServiceServer).Retrieve(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: EmbeddingService_Retrieve_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(EmbeddingServiceServer).Retrieve(ctx, req.(*RetrieveRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// EmbeddingService_ServiceDesc is the grpc.ServiceDesc for EmbeddingService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var EmbeddingService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "udb.core.embedding.services.v1.EmbeddingService",
	HandlerType: (*EmbeddingServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "RegisterSource",
			Handler:    _EmbeddingService_RegisterSource_Handler,
		},
		{
			MethodName: "ListSources",
			Handler:    _EmbeddingService_ListSources_Handler,
		},
		{
			MethodName: "DeleteSource",
			Handler:    _EmbeddingService_DeleteSource_Handler,
		},
		{
			MethodName: "Backfill",
			Handler:    _EmbeddingService_Backfill_Handler,
		},
		{
			MethodName: "ReportEmbedding",
			Handler:    _EmbeddingService_ReportEmbedding_Handler,
		},
		{
			MethodName: "Retrieve",
			Handler:    _EmbeddingService_Retrieve_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "udb/core/embedding/services/v1/embedding_service.proto",
}
