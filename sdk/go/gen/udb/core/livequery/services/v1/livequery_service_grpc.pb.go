// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: udb/core/livequery/services/v1/livequery_service.proto

package servicesv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	LiveQueryService_Subscribe_FullMethodName = "/udb.core.livequery.services.v1.LiveQueryService/Subscribe"
)

// LiveQueryServiceClient is the client API for LiveQueryService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// LiveQueryService (master-plan 9.7) — query results that update themselves. A
// client subscribes to a tenant-scoped query over a source entity and receives
// an initial Snapshot (the current matching rows) followed by an open stream of
// Change deltas (insert / update / delete) as the underlying data mutates.
//
// Tenant isolation is the whole point: the snapshot is produced ONLY through the
// mediated IR read path with the tenant predicate injected server-side from the
// verified claim (never a raw query), and EVERY delta event is re-checked, fail
// closed, against the subscriber's tenant scope before it is yielded — a CDC
// event with a missing or foreign tenant_id is dropped, never streamed. The
// subscription filter is an IR-expressible predicate set, not a raw query
// string, so no caller-supplied SQL ever reaches a backend.
type LiveQueryServiceClient interface {
	// Subscribe to a tenant-scoped live query. SERVER-STREAMING: the first message
	// carries the initial Snapshot (the current rows matching the IR filter, read
	// through the mediated path with the tenant predicate injected server-side);
	// every subsequent message carries a single Change delta. Fails closed
	// (failed_precondition) when the source entity has no resolvable tenant column.
	Subscribe(ctx context.Context, in *SubscribeRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[SubscribeResponse], error)
}

type liveQueryServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewLiveQueryServiceClient(cc grpc.ClientConnInterface) LiveQueryServiceClient {
	return &liveQueryServiceClient{cc}
}

func (c *liveQueryServiceClient) Subscribe(ctx context.Context, in *SubscribeRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[SubscribeResponse], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &LiveQueryService_ServiceDesc.Streams[0], LiveQueryService_Subscribe_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[SubscribeRequest, SubscribeResponse]{ClientStream: stream}
	if err := x.ClientStream.SendMsg(in); err != nil {
		return nil, err
	}
	if err := x.ClientStream.CloseSend(); err != nil {
		return nil, err
	}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type LiveQueryService_SubscribeClient = grpc.ServerStreamingClient[SubscribeResponse]

// LiveQueryServiceServer is the server API for LiveQueryService service.
// All implementations should embed UnimplementedLiveQueryServiceServer
// for forward compatibility.
//
// LiveQueryService (master-plan 9.7) — query results that update themselves. A
// client subscribes to a tenant-scoped query over a source entity and receives
// an initial Snapshot (the current matching rows) followed by an open stream of
// Change deltas (insert / update / delete) as the underlying data mutates.
//
// Tenant isolation is the whole point: the snapshot is produced ONLY through the
// mediated IR read path with the tenant predicate injected server-side from the
// verified claim (never a raw query), and EVERY delta event is re-checked, fail
// closed, against the subscriber's tenant scope before it is yielded — a CDC
// event with a missing or foreign tenant_id is dropped, never streamed. The
// subscription filter is an IR-expressible predicate set, not a raw query
// string, so no caller-supplied SQL ever reaches a backend.
type LiveQueryServiceServer interface {
	// Subscribe to a tenant-scoped live query. SERVER-STREAMING: the first message
	// carries the initial Snapshot (the current rows matching the IR filter, read
	// through the mediated path with the tenant predicate injected server-side);
	// every subsequent message carries a single Change delta. Fails closed
	// (failed_precondition) when the source entity has no resolvable tenant column.
	Subscribe(*SubscribeRequest, grpc.ServerStreamingServer[SubscribeResponse]) error
}

// UnimplementedLiveQueryServiceServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedLiveQueryServiceServer struct{}

func (UnimplementedLiveQueryServiceServer) Subscribe(*SubscribeRequest, grpc.ServerStreamingServer[SubscribeResponse]) error {
	return status.Error(codes.Unimplemented, "method Subscribe not implemented")
}
func (UnimplementedLiveQueryServiceServer) testEmbeddedByValue() {}

// UnsafeLiveQueryServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to LiveQueryServiceServer will
// result in compilation errors.
type UnsafeLiveQueryServiceServer interface {
	mustEmbedUnimplementedLiveQueryServiceServer()
}

func RegisterLiveQueryServiceServer(s grpc.ServiceRegistrar, srv LiveQueryServiceServer) {
	// If the following call panics, it indicates UnimplementedLiveQueryServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&LiveQueryService_ServiceDesc, srv)
}

func _LiveQueryService_Subscribe_Handler(srv interface{}, stream grpc.ServerStream) error {
	m := new(SubscribeRequest)
	if err := stream.RecvMsg(m); err != nil {
		return err
	}
	return srv.(LiveQueryServiceServer).Subscribe(m, &grpc.GenericServerStream[SubscribeRequest, SubscribeResponse]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type LiveQueryService_SubscribeServer = grpc.ServerStreamingServer[SubscribeResponse]

// LiveQueryService_ServiceDesc is the grpc.ServiceDesc for LiveQueryService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var LiveQueryService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "udb.core.livequery.services.v1.LiveQueryService",
	HandlerType: (*LiveQueryServiceServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Subscribe",
			Handler:       _LiveQueryService_Subscribe_Handler,
			ServerStreams: true,
		},
	},
	Metadata: "udb/core/livequery/services/v1/livequery_service.proto",
}
