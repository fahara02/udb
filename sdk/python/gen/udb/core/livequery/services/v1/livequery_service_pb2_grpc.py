# Generated by the gRPC Python protocol compiler plugin. DO NOT EDIT!
"""Client and server classes corresponding to protobuf-defined services."""
import grpc

from udb.core.livequery.services.v1 import livequery_service_pb2 as udb_dot_core_dot_livequery_dot_services_dot_v1_dot_livequery__service__pb2


class LiveQueryServiceStub(object):
    """LiveQueryService (master-plan 9.7) — query results that update themselves. A
    client subscribes to a tenant-scoped query over a source entity and receives
    an initial Snapshot (the current matching rows) followed by an open stream of
    Change deltas (insert / update / delete) as the underlying data mutates.

    Tenant isolation is the whole point: the snapshot is produced ONLY through the
    mediated IR read path with the tenant predicate injected server-side from the
    verified claim (never a raw query), and EVERY delta event is re-checked, fail
    closed, against the subscriber's tenant scope before it is yielded — a CDC
    event with a missing or foreign tenant_id is dropped, never streamed. The
    subscription filter is an IR-expressible predicate set, not a raw query
    string, so no caller-supplied SQL ever reaches a backend.
    """

    def __init__(self, channel):
        """Constructor.

        Args:
            channel: A grpc.Channel.
        """
        self.Subscribe = channel.unary_stream(
                '/udb.core.livequery.services.v1.LiveQueryService/Subscribe',
                request_serializer=udb_dot_core_dot_livequery_dot_services_dot_v1_dot_livequery__service__pb2.SubscribeRequest.SerializeToString,
                response_deserializer=udb_dot_core_dot_livequery_dot_services_dot_v1_dot_livequery__service__pb2.SubscribeResponse.FromString,
                _registered_method=True)


class LiveQueryServiceServicer(object):
    """LiveQueryService (master-plan 9.7) — query results that update themselves. A
    client subscribes to a tenant-scoped query over a source entity and receives
    an initial Snapshot (the current matching rows) followed by an open stream of
    Change deltas (insert / update / delete) as the underlying data mutates.

    Tenant isolation is the whole point: the snapshot is produced ONLY through the
    mediated IR read path with the tenant predicate injected server-side from the
    verified claim (never a raw query), and EVERY delta event is re-checked, fail
    closed, against the subscriber's tenant scope before it is yielded — a CDC
    event with a missing or foreign tenant_id is dropped, never streamed. The
    subscription filter is an IR-expressible predicate set, not a raw query
    string, so no caller-supplied SQL ever reaches a backend.
    """

    def Subscribe(self, request, context):
        """Subscribe to a tenant-scoped live query. SERVER-STREAMING: the first message
        carries the initial Snapshot (the current rows matching the IR filter, read
        through the mediated path with the tenant predicate injected server-side);
        every subsequent message carries a single Change delta. Fails closed
        (failed_precondition) when the source entity has no resolvable tenant column.
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')


def add_LiveQueryServiceServicer_to_server(servicer, server):
    rpc_method_handlers = {
            'Subscribe': grpc.unary_stream_rpc_method_handler(
                    servicer.Subscribe,
                    request_deserializer=udb_dot_core_dot_livequery_dot_services_dot_v1_dot_livequery__service__pb2.SubscribeRequest.FromString,
                    response_serializer=udb_dot_core_dot_livequery_dot_services_dot_v1_dot_livequery__service__pb2.SubscribeResponse.SerializeToString,
            ),
    }
    generic_handler = grpc.method_handlers_generic_handler(
            'udb.core.livequery.services.v1.LiveQueryService', rpc_method_handlers)
    server.add_generic_rpc_handlers((generic_handler,))
    server.add_registered_method_handlers('udb.core.livequery.services.v1.LiveQueryService', rpc_method_handlers)


 # This class is part of an EXPERIMENTAL API.
class LiveQueryService(object):
    """LiveQueryService (master-plan 9.7) — query results that update themselves. A
    client subscribes to a tenant-scoped query over a source entity and receives
    an initial Snapshot (the current matching rows) followed by an open stream of
    Change deltas (insert / update / delete) as the underlying data mutates.

    Tenant isolation is the whole point: the snapshot is produced ONLY through the
    mediated IR read path with the tenant predicate injected server-side from the
    verified claim (never a raw query), and EVERY delta event is re-checked, fail
    closed, against the subscriber's tenant scope before it is yielded — a CDC
    event with a missing or foreign tenant_id is dropped, never streamed. The
    subscription filter is an IR-expressible predicate set, not a raw query
    string, so no caller-supplied SQL ever reaches a backend.
    """

    @staticmethod
    def Subscribe(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_stream(
            request,
            target,
            '/udb.core.livequery.services.v1.LiveQueryService/Subscribe',
            udb_dot_core_dot_livequery_dot_services_dot_v1_dot_livequery__service__pb2.SubscribeRequest.SerializeToString,
            udb_dot_core_dot_livequery_dot_services_dot_v1_dot_livequery__service__pb2.SubscribeResponse.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)
