# UDB Python SDK

Generate protobuf clients first:

```bash
../scripts/gen_sdk.sh
```

The async wrapper in `udb_client/client.py` adds required UDB metadata and exposes common OCR service operations.
