{
  "name": "udb-ts-enterprise-example",
  "private": true,
  "version": "0.0.0",
  "description": "Standalone UDB enterprise example: real JWT login + ABAC authorization + tenant-scoped CRUD, using the installed @udb_plus/sdk.",
  "scripts": {
    "gen": "udb proto export --out proto && buf generate --path proto/billing/v1/invoice.proto",
    "start": "tsx src/main.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@udb_plus/sdk": "^0.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "tsx": "^4.0.0",
    "typescript": "^5.7.2"
  }
}
