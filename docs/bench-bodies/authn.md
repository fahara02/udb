## AuthnService

_proto: core/authn/services/v1/authn_service.proto · 51 RPCs_

All request/response message bodies live in `core/authn/services/v1/core.proto`. Enum fields resolve against `core/authn/entity/v1/enums.proto`; `context` is `udb.core.common.v1.RequestContext` (from `common/v1/types.proto`, fields: `tenant{TenantContext}`, `request_id`, `correlation_id`, `user_id`, `headers`, `trace_id`, `ip_address`, `user_agent`, `idempotency_key`, `scopes`, `roles`, ...). Most authn fields read from `RequestContext` are populated server-side from the bearer/session, so the column below lists only the *message body* fields you set.

| done | RPC | op_kind | request msg | valid body | seed refs / notes |
| --- | --- | --- | --- | --- | --- |
| [ ] | AdminResetMfa | DESTRUCTIVE | AdminResetMfaRequest | `{ "user_id": "<seed:admin_reset_mfa_user_id>", "reason": "perf" }` | Uses a disposable seeded user so terminal MFA reset does not poison other authn measurements. |
| [ ] | AdminResetPassword | DESTRUCTIVE | AdminResetPasswordRequest | `{ "user_id": "<seed:admin_reset_password_user_id>" }` | Sends email OTP for a disposable seeded user. Optional `context`. |
| [ ] | AdminRevokeAllTenantSessions | DESTRUCTIVE | AdminRevokeAllTenantSessionsRequest | `{ "tenant_id": "<seed:tenant_id>", "reason": "perf" }` | Optional `context`. Terminal Phase 3 row: targets the verified bench tenant only after the measured non-terminal rows have run. |
| [ ] | AdminRevokeAllUserSessions | DESTRUCTIVE | AdminRevokeAllUserSessionsRequest | `{ "user_id": "<seed:user_id>", "reason": "perf" }` | Optional `context`. |
| [ ] | AdminRevokeSession | DESTRUCTIVE | AdminRevokeSessionRequest | `{ "user_id": "<seed:user_id>", "session_id": "<seed:session_id>", "reason": "perf" }` | Optional `context`. |
| [ ] | Authenticate | READ_ONLY | AuthnRequest | `{ "bearer_token": "<seed:token>", "credential_type": "AUTH_CREDENTIAL_TYPE_BEARER_TOKEN" }` | PUBLIC. Provide ONE proof: `bearer_token` / `session_id` / `api_key` / (`external_provider_id`+`external_token`). `credential_type` enum (AuthCredentialType). Optional `tenant_hint`, `project_hint`, `requested_scopes`, `client_id`, `audience`, `issuer`, `attributes`. |
| [ ] | ChangePassword | MUTATION | ChangePasswordRequest | `{ "user_id": "<seed:change_password_user_id>", "current_password": "CorrectHorse1!", "new_password": "N3w!Passw0rd9" }` | Uses a disposable seeded user whose current password matches the seed password. |
| [ ] | ChangeUserStatus | DESTRUCTIVE | ChangeUserStatusRequest | `{ "user_id": "<seed:change_status_user_id>", "new_status": "USER_STATUS_SUSPENDED", "reason": "perf" }` | Suspends a disposable seeded user so the primary session user remains active. |
| [ ] | ConfirmMFAEnrollment | MUTATION | ConfirmMFAEnrollmentRequest | `{ "user_id": "<seed:user_id>", "otp_id": "<seed:code>", "code": "123456" }` | `otp_id` = verify_otp_id from EnrollMFA; `code` = TOTP/email code. NOTE: correct TOTP code not groundable from proto (computed from totp_secret at runtime). |
| [ ] | CreateCertificateBinding | MUTATION | CreateCertificateBindingRequest | `{ "tenant_id": "<seed:tenant_id>", "user_id": "<seed:owner_id>", "selector_kind": "SPIFFE_URI", "selector_value": "spiffe://bench/svc-grant", "scope_subset": [], "reason": "bench binding" }` | Requires an ACTIVE grant; empty scope_subset = full current grant (never widening). |
| [ ] | CreateServiceAccountGrant | MUTATION | CreateServiceAccountGrantRequest | `{ "tenant_id": "<seed:tenant_id>", "user_id": "<seed:grant_create_user_id>", "service_identity": "spiffe://bench/svc-grant-b", "project_id": "<seed:project>", "approved_scopes": ["udb:read", "udb:write"], "reason": "bench seed grant" }` | fix_plan §2: typed operator-approved grant; admin/wildcard scopes are rejected at write. |
| [ ] | CreateSession | MUTATION | CreateSessionRequest | `{ "principal": { "principal_id": "<seed:user_id>", "subject": "<seed:subject>", "user_id": "<seed:user_id>", "tenant_id": "<seed:tenant_id>" }, "ttl_seconds": 3600 }` | Nested `Principal` message (expand required ids). Optional `client_fingerprint`. |
| [ ] | CreateUser | MUTATION | CreateUserRequest | `{ "username": "perf-u", "email": "perf-u@acme.test", "password": "Str0ng!Passw0rd", "tenant_id": "<seed:tenant_id>", "full_name": "Perf U", "account_kind": "ACCOUNT_KIND_PERSON" }` | password policy: min 10, 1 upper/lower/digit/special. `account_kind` enum (AccountKind). Optional: `project_id`, `external_provider_id`, `external_subject`, `profile_attributes` map, `context`. |
| [ ] | DeleteWebAuthnCredential | MUTATION | DeleteWebAuthnCredentialRequest | `{ "user_id": "<seed:user_id>", "credential_id": "<seed:record_id>" }` | `credential_id` ref (WebAuthn credential id; no dedicated seed key — use record_id). Optional `context`. |
| [ ] | DisableMfaFactor | MUTATION | DisableMfaFactorRequest | `{ "user_id": "<seed:disable_mfa_user_id>", "factor_kind": "AUTH_FACTOR_KIND_TOTP" }` | Uses a disposable seeded user. `factor_kind` enum (AuthFactorKind, non-UNSPECIFIED). |
| [ ] | EmergencyRevoke | DESTRUCTIVE | EmergencyRevokeRequest | `{ "principal_id": "<seed:subject>", "reason": "perf" }` | Provide at least one revoke target: `signing_key_id` (<seed:key_id>) / `token_family_id` / `tenant_id` / `principal_id` (<seed:subject>). This bench row targets only the seeded disposable principal, not the whole active tenant. Optional `context`. |
| [ ] | EnrollMFA | MUTATION | EnrollMFARequest | `{ "user_id": "<seed:user_id>", "mfa_type": "AUTH_FACTOR_KIND_TOTP" }` | `mfa_type` enum (AuthFactorKind, non-UNSPECIFIED). Optional `context`. |
| [ ] | FinishWebAuthnAuthentication | MUTATION | FinishWebAuthnAuthenticationRequest | `{ "challenge_id": "<seed:auth_challenge_id>", "public_key_credential_json": "__UDB_WEBAUTHN_TEST__" }` | PUBLIC. `challenge_id` from Start...; the live harness uses the dev soft-authenticator sentinel credential. Optional `context`. |
| [ ] | FinishWebAuthnRegistration | MUTATION | FinishWebAuthnRegistrationRequest | `{ "challenge_id": "<seed:reg_challenge_id>", "public_key_credential_json": "__UDB_WEBAUTHN_TEST__", "label": "perf-key" }` | `challenge_id` from Start...; the live harness uses the dev soft-authenticator sentinel credential. Optional `context`. |
| [ ] | ForgotPassword | MUTATION | ForgotPasswordRequest | `{ "identifier": "perf-u@acme.test" }` | PUBLIC. `identifier` = username or email. Optional `context`. |
| [ ] | GenerateRecoveryCodes | MUTATION | GenerateRecoveryCodesRequest | `{ "user_id": "<seed:user_id>", "count": 10 }` | `count` clamped server-side (default 10). Optional `context`. |
| [ ] | GetJwks | READ_ONLY | GetJwksRequest | `{}` | PUBLIC. Only optional `context`; no required fields. |
| [ ] | GetMfaPolicy | READ_ONLY | GetMfaPolicyRequest | `{ "tenant_id": "<seed:tenant_id>" }` | Optional `context`. |
| [ ] | GetServiceAccountGrant | READ_ONLY | GetServiceAccountGrantRequest | `{ "tenant_id": "<seed:tenant_id>", "user_id": "<seed:owner_id>" }` | Reads the typed grant for the SEEDED SERVICE ACCOUNT (`<seed:owner_id>` — the human `<seed:user_id>` has no grant). |
| [ ] | GetSession | READ_ONLY | GetSessionRequest | `{ "session_id": "<seed:session_id>" }` | session_id ref. |
| [ ] | GetUser | READ_ONLY | GetUserRequest | `{ "user_id": "<seed:user_id>" }` | Lookup-by oneof-style: provide ONE of `user_id` / `username` / `email` (not a proto oneof, but only one needed). |
| [ ] | IntrospectToken | READ_ONLY | IntrospectTokenRequest | `{ "token": "<seed:token>" }` | Optional `context`. |
| [ ] | IssueMfaChallenge | MUTATION | IssueMfaChallengeRequest | `{ "user_id": "<seed:user_id>", "factor_kind": "AUTH_FACTOR_KIND_TOTP", "purpose": "MFA_CHALLENGE_PURPOSE_SENSITIVE_OPERATION" }` | `factor_kind` enum (AuthFactorKind), `purpose` enum (MfaChallengePurpose), both non-UNSPECIFIED. Optional `device_fingerprint`, `ip_address`, `context`. |
| [ ] | ListCertificateBindings | READ_ONLY | ListCertificateBindingsRequest | `{ "tenant_id": "<seed:tenant_id>", "page_size": 50 }` | Numeric-offset page_token; next_page_token on full pages. |
| [ ] | ListDevices | READ_ONLY | ListDevicesRequest | `{ "user_id": "<seed:user_id>" }` | Optional `page` (PageRequest), `context`. |
| [ ] | ListMfaFactors | READ_ONLY | ListMfaFactorsRequest | `{ "user_id": "<seed:user_id>" }` | Optional `context`. |
| [ ] | ListServiceAccountGrants | READ_ONLY | ListServiceAccountGrantsRequest | `{ "tenant_id": "<seed:tenant_id>", "page_size": 50 }` | Numeric-offset page_token; next_page_token on full pages. |
| [ ] | ListSessions | READ_ONLY | ListSessionsRequest | `{ "user_id": "<seed:user_id>", "active_only": true, "page": { "page": 1, "page_size": 20 } }` | Optional `active_only: true`, `page` (PageRequest). |
| [ ] | ListUsers | READ_ONLY | ListUsersRequest | `{ "tenant_id": "<seed:tenant_id>", "page": { "page": 1, "page_size": 20 } }` | Optional filters: `account_kind` (AccountKind), `status` (UserStatus), `page` (PageRequest). |
| [ ] | ListWebAuthnCredentials | READ_ONLY | ListWebAuthnCredentialsRequest | `{ "user_id": "<seed:user_id>" }` | Optional `context`. |
| [ ] | Login | MUTATION | LoginRequest | `{ "username": "<seed:username>", "password": "CorrectHorse1!", "device_type": "DEVICE_TYPE_API", "device_name": "go-perf", "tenant_hint": "<seed:tenant_id>", "project_hint": "<seed:project>" }` | PUBLIC. `device_type` enum (DeviceType, non-UNSPECIFIED). MFA step-2 fields (`mfa_otp_id`,`totp_code`,`recovery_code`) only on second call after `mfa_required=true`. Optional `ip_address`,`user_agent`,`device_id`,`access_surface`. |
| [ ] | Logout | MUTATION | LogoutRequest | `{ "session_id": "<seed:session_id>" }` | Optional `all_sessions: true` to revoke all, `revoke_reason`, `context`. |
| [ ] | PutMfaPolicy | MUTATION | PutMfaPolicyRequest | `{ "tenant_id": "<seed:tenant_id>", "require_mfa": false }` | Optional `context`. `require_mfa` is deliberately false for the shared live bench tenant; enabling it poisons later Login measurements because the admin seed user has no enrolled second factor. |
| [ ] | RefreshSession | MUTATION | RefreshSessionRequest | `{ "session_id": "<seed:refresh_session_id>", "ttl_seconds": 3600 }` | session_id ref. Uses the dedicated refresh-session seed so RefreshToken rotation does not invalidate it. |
| [ ] | RefreshToken | MUTATION | RefreshTokenRequest | `{ "refresh_token": "<seed:refresh_token>" }` | PUBLIC. Provide `refresh_token` (token-family credential) OR legacy `session_id`. |
| [ ] | RenamePasskey | MUTATION | RenamePasskeyRequest | `{ "user_id": "<seed:user_id>", "credential_id": "<seed:record_id>", "new_label": "perf-key2" }` | `credential_id` ref (WebAuthn credential id; no dedicated seed key — use record_id). Optional `context`. |
| [ ] | ReplaceServiceAccountGrant | MUTATION | ReplaceServiceAccountGrantRequest | `{ "tenant_id": "<seed:tenant_id>", "user_id": "<seed:owner_id>", "approved_scopes": ["udb:read", "data:read", "resource:read"], "reason": "bench replace", "expected_revision": 1 }` | Revision CAS: mismatch fails FAILED_PRECONDITION; revision bumps on success. |
| [ ] | ResendOTP | MUTATION | ResendOTPRequest | `{ "original_otp_id": "<seed:otp_id>", "reason": "not_received" }` | `original_otp_id` refs a prior OTP id. `reason` is free string (suggested: not_received \|expired \|delivery_failed). |
| [ ] | ResetPassword | MUTATION | ResetPasswordRequest | `{ "otp_id": "<seed:reset_otp_id>", "code": "<seed:reset_otp_code>", "new_password": "N3w!Passw0rd9" }` | PUBLIC. `otp_id`/`code` from a runtime-issued PASSWORD_RESET OTP. Optional `context`. |
| [ ] | RevokeCertificateBinding | DESTRUCTIVE | RevokeCertificateBindingRequest | `{ "tenant_id": "<seed:tenant_id>", "binding_id": "<seed:grant_binding_id>", "reason": "bench revoke" }` | binding_id from CreateCertificateBinding (server-assigned UUID); revoked bindings fail closed at the TLS gate. |
| [ ] | RevokeDevice | MUTATION | RevokeDeviceRequest | `{ "device_id": "<seed:revoke_device_id>", "reason": "perf" }` | Revokes a disposable Login-created device row. |
| [ ] | RevokeRecoveryCodes | MUTATION | RevokeRecoveryCodesRequest | `{ "user_id": "<seed:revoke_recovery_user_id>" }` | Uses a disposable seeded user. |
| [ ] | RevokeServiceAccountGrant | DESTRUCTIVE | RevokeServiceAccountGrantRequest | `{ "tenant_id": "<seed:tenant_id>", "user_id": "<seed:owner_id>", "reason": "bench revoke" }` | Idempotent; only ACTIVE grants flip. |
| [ ] | RevokeSession | MUTATION | RevokeSessionRequest | `{ "session_id": "<seed:session_id>", "revoke_reason": "perf" }` | Or revoke all for a principal: `principal_id: "<seed:subject>"`, `all_for_principal: true`. Optional `context`. |
| [ ] | RotateServiceAccountIdentity | DESTRUCTIVE | RotateServiceAccountIdentityRequest | `{ "tenant_id": "<seed:tenant_id>", "user_id": "<seed:grant_create_user_id>", "new_service_identity": "spiffe://bench/svc-grant-rotated", "expected_revision": 1, "reason": "bench rotate identity" }` | Revision CAS: rebinds the grant to a new service identity; supersedes prior certificate bindings. |
| [ ] | SendOTP | MUTATION | SendOTPRequest | `{ "user_id": "<seed:user_id>", "otp_type": "OTP_TYPE_EMAIL_VERIFICATION" }` | `otp_type` enum (OTPType, non-UNSPECIFIED). Optional `correlation_id`, `context`. |
| [ ] | SendPhoneVerification | MUTATION | SendPhoneVerificationRequest | `{ "user_id": "<seed:user_id>", "phone": "+15551234567" }` | `phone` = E.164. Complete with VerifyOTP. Optional `context`. |
| [ ] | StartWebAuthnAuthentication | MUTATION | StartWebAuthnAuthenticationRequest | `{ "user_id": "<seed:user_id>", "tenant_id": "<seed:tenant_id>" }` | PUBLIC. Optional `project_id`, `context`. |
| [ ] | StartWebAuthnRegistration | MUTATION | StartWebAuthnRegistrationRequest | `{ "user_id": "<seed:user_id>", "label": "perf-key", "tenant_id": "<seed:tenant_id>" }` | Optional `project_id`, `context`. |
| [ ] | TransferServiceAccountGrant | DESTRUCTIVE | TransferServiceAccountGrantRequest | `{ "tenant_id": "<seed:tenant_id>", "from_user_id": "<seed:owner_id>", "to_user_id": "<seed:grant_transfer_to_user_id>", "expected_revision": 1, "reason": "bench transfer grant" }` | Revision CAS: moves the ACTIVE grant from `from_user_id` to a grantless ACTIVE service account (`to_user_id`) in the same tenant/project; the grant's revision bumps on success while its service_identity/scopes are unchanged. Terminal grant row — run after the other grant measurements. |
| [ ] | UpdateUser | MUTATION | UpdateUserRequest | `{ "user_id": "<seed:user_id>", "full_name": "Perf U2", "email": "perf-u2@acme.test", "tenant_id": "<seed:tenant_id>" }` | Optional: `account_kind` (AccountKind), `project_id`, `profile_attributes`, `external_provider_id`, `external_subject`, `context`. |
| [ ] | ValidateCSRF | READ_ONLY | ValidateCSRFRequest | `{ "session_id": "<seed:session_id>", "csrf_token": "<seed:csrf_token>" }` | Server-side sessions only. csrf_token = value from csrf cookie/header (runtime-issued at Login). |
| [ ] | ValidateToken | READ_ONLY | ValidateTokenRequest | `{ "token": "<seed:token>", "token_type": "TOKEN_TYPE_JWT_ACCESS" }` | `token_type` enum (TokenType, non-UNSPECIFIED). `token` = raw JWT or session_token. |
| [ ] | VerifyMfaChallenge | READ_ONLY | VerifyMfaChallengeRequest | `{ "challenge_id": "<seed:challenge_id>", "code": "<seed:otp_code>" }` | `challenge_id` from IssueMfaChallenge; `code` = TOTP/OTP/recovery proof (runtime-dependent). Optional `device_fingerprint`, `context`. |
| [ ] | VerifyOTP | READ_ONLY | VerifyOTPRequest | `{ "otp_id": "<seed:otp_id>", "code": "<seed:otp_code>" }` | `otp_id` is an OTP-handle ref (depends on a prior SendOTP); `code` is the 6-digit plaintext. NOTE: a *correct* code cannot be grounded from proto alone — depends on the OTP issued at runtime. |
